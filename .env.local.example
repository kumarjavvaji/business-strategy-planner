# Copy this file to .env.local and fill in your key.
# .env.local is git-ignored — never commit real keys.
#
# Stage 2 will work in mock mode if this key is missing.
# Set to enable AI-assisted business-unit generation via Anthropic Claude.
# After creating or editing .env.local, restart the dev server (npm run dev).

VITE_ANTHROPIC_API_KEY=sk-ant-...
